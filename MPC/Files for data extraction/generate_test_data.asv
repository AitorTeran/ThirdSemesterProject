number_test_data=1e6;


close all

%%

apool = gcp('nocreate');
if isempty(apool)
    apool = parpool('local');
end

%%

x_opt_old = zeros (1, number_test_data);
vref_alpha = zeros (1, number_test_data);
vref_beta = zeros (1, number_test_data);
vc_alpha = zeros (1, number_test_data);
vc_beta =  zeros (1, number_test_data);
if_alpha = zeros (1, number_test_data);
if_beta = zeros (1, number_test_data);
R = zeros (1, number_test_data);



x_opt_old = unidrnd(7,[number_test_data,1]) %it creates discrete numbers until 7 without 0

%rand generates from 0-1 using uniform distribution, multiplied by
%amplitude -/+ y0 value gives us the correct random range
time=rand(number_test_data,1)*0.018;
vc_alpha = rand(number_test_data,1)*10-5;
vc_beta =  rand(number_test_data,1)*10-5;
if_alpha = rand(number_test_data,1)*32-16;
if_beta = rand(number_test_data,1)*32-16;
R = rand(number_test_data,1)*30+30;

save